spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/taskflowpro
    username: taskflow
    password: taskflow
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
